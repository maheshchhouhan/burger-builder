import React from "react";

import "./DrawerToggle.css";

const drawerToggle = ({ drawerToggleClicked }) => (
  <div className="DrawerToggle" onClick={drawerToggleClicked}>
    <div></div>
    <div></div>
    <div></div>
  </div>
);

export default drawerToggle;
